having a command in a hook step still needs thinking.
e.g. what / how to send feedback to the agent if it's an md command that's run?


- does the plan mode store a verbose enough plan? it prints a lot but does that get stored to the json as well?